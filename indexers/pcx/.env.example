# Multi-Chain Indexer Configuration

# Avalanche RPC URL (default: https://api.avax.network)
# For a private node set to http://localhost:9650
RPC_URL=https://api.avax.network

# P-Chain / X-Chain: blocks to fetch in parallel per batch (default: 10)
# Higher = faster sync but more connections
# Recommended: 100-1000 for private RPC
BATCH_SIZE=10

# C-Chain: blocks per batch JSON-RPC request (default: 1000)
# Each request fetches this many blocks in one call
C_BATCH_SIZE=1000

# C-Chain: number of parallel batch requests (default: 10)
# Total blocks per cycle = C_BATCH_SIZE * C_PARALLEL
C_PARALLEL=10

# Poll interval in milliseconds when caught up (default: 1000)
# Recommended: 100ms for private RPC
POLL_INTERVAL_MS=1000

# Glacier API key for scraping pre-Cortina timestamps (cmd/scrape_timestamps)
# Only needed for one-time historical data scrape
# DATA_API_KEY=your_glacier_api_key_here
